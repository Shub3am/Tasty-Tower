<div class="header">
  <div class="social-links">
    <img src="facebook.png" width="45" />
    <img src="youtube.png" width="45" />
  </div>
  <div class="logo">
    <img src="logo.png" width="300" />
  </div>
  <div class="menu">
    <nav>
      <ul>
        <li>Home</li>
        <li>Blog</li>
        <li>About</li>
        <li>Contact</li>
      </ul>
    </nav>
  </div>
  <div class="mobileMenu">
    <button id="openBtn"><img src="nav.png" width="35" role="button" /></button>
    <nav class="mobileNav">
      <ul>
        <div id="closeBtn">
          <img src="close.png" width="35" role="button" />
        </div>
        <li>Home</li>
        <li>Blog</li>
        <li>About</li>
        <li>Contact</li>
      </ul>
    </nav>
  </div>
</div>
<script>
  let isOpen = false;
  const mobileNav = document.querySelector(".mobileNav");
  const openBtn = document.querySelector("#openBtn");
  const closeBtn = document.querySelector("#closeBtn");
  openBtn.addEventListener("click", () => {
    mobileNav?.classList.toggle("isOpen");
  });
  closeBtn.addEventListener("click", () =>
    mobileNav?.classList.toggle("isOpen")
  );
</script>

<style>
  .header {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    background: #f0f0f0;
    padding: 1em;
  }
  .mobileMenu {
    display: none;
  }
  .social-links {
    display: flex;
    justify-content: center;
    align-items: center;
  }
  .social-links img {
    margin: 0 5px;
  }
  .logo {
    display: flex;
    align-content: center;
    justify-content: center;
  }
  .menu {
    display: flex;
    justify-content: center;
    align-items: center;
  }
  .menu ul {
    display: flex;
  }
  .menu li {
    list-style: none;
    margin: 0 10px;
    font-size: 20px;
  }
  @media all and (max-width: 900px) {
    .social-links,
    .menu {
      display: none;
    }
    .header {
      grid-template-columns: 1fr 1fr;
    }
    .mobileMenu {
      display: grid;
      grid-template-columns: 1fr 0;
    }
    .mobileNav {
      position: fixed;
      display: none;
      right: 0;
      height: 100%;
      top: 0;
      width: 8em;
      background: black;
      color: white;
    }
    .isOpen {
      display: block;
    }
    .mobileNav li {
      font-size: 1.5em;
      padding: 1em;
    }
    #closeBtn {
      display: flex;
      justify-content: center;
      padding: 1em;
    }
  }
</style>
